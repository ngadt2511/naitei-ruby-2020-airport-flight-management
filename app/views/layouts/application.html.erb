<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title yield :title %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="turbolinks-visit-control" content="reload">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>

    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track": "reload" %>
    <%= javascript_pack_tag "application", "data-turbolinks-track": "reload" %>
    <%= javascript_tag do %>
      I18n.defaultLocale = "<%= I18n.default_locale %>";
      I18n.locale = "<%= I18n.locale %>";
    <% end %>
    <link href=<%= Settings.link.app.fontawesome %> rel="stylesheet">
  </head>

  <body>
    <div class="page-wrapper chiller-theme toggled" id="app">
      <%= render "layouts/sidebar" %>
      <main class="page-content">
        <%= render "layouts/header" %>
        <%= render "layouts/messages" %>
        <div class="main-container">
          <% if current_user %>
            <span class="hidden-box" id=<%= "type_account_#{current_user.type_account}" %> email=<%= current_user.email %>></span>
            <span class="hidden-box" id=<%= "owner_notification_#{current_user.id}" %>></span>
          <% end %>
          <%= yield %>
          <%= render "layouts/alert" %>
          <button id="btn-show-notification" class="hidden-box">
          </button>
        </div>
      </main>
    </div>
  </body>
</html>
